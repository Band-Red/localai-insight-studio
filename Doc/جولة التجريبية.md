سنقوم الآن بجولة فحص (Walkthrough) سريعة وشاملة للتأكد من أن كل شيء يعمل كما هو مخطط له، خاصة الأجزاء التي قمنا بتعديلها مؤخراً.

الخطوة 1: فحص نبض النظام (لوحة التحكم - Dashboard)
انتقل إلى تبويب "لوحة التحكم".
ما الذي يجب ملاحظته: تأكد من أن الرسم البياني يتحرك وتظهر نسب استهلاك المعالج (CPU) والرام (RAM).
ملاحظة: إذا ظهرت البيانات، فهذا يعني أن نظام الـ Python Bridge وتعديل الـ Fallback الذي قمنا به يعمل بنجاح على جهازك.
الخطوة 2: تهيئة النموذج والذكاء الإضافي (Model Manager)
انتقل إلى تبويب "النماذج".
قم بإضافة نموذج GGUF (عبر السحب والإفلات أو اختيار ملف).
انتقل إلى فرع "الإعدادات" (Parameters) داخل نفس الصفحة.
افتح قائمة "Draft Model (اختياري)".
الفحص: ستلاحظ الآن ظهور النماذج الموجودة في مكتبتك ( Library) داخل القائمة، مما يتيح لك تفعيل تقنية التوليد السريع (Speculative Decoding).
الخطوة 3: اختبار الدردشة والبحث في الملفات (Chat & RAG)
انتقل إلى "الدردشة".
انقر على أيقونة "مشبك الورق" (Paperclip) في الأسفل.
اختر مجلد مشروع أو ملف برمجي.
النتيجة المتوقعة: سيظهر لك شريط علوي صغير مكتوب فيه "📎 تم إرفاق سياق محلي (X ملفات)". هذا يؤكد أن منطق الفهرسة (Indexing) الجديد الذي فعلناه يعمل.
اسأل الذكاء الاصطناعي: "اشرح لي الكود الموجود في الملفات المرفقة".
الخطوة 4: تجربة المحرر البصري (Previewer & Visual Editor)
افتح صفحة "المعاينة" (Previewer).
قم بتوليد كود بسيط (مثل صفحة HTML).
اضغط على أيقونة "المفتش" (Inspect) ثم قف فوق أي عنصر في الصفحة.
عند ظهور نافذة التعديل الصغيرة (Overlay):
الفحص: جرب الكتابة داخل الخانة التي تظهر فوق العنصر. (لقد أصلحنا مشكلة عدم القدرة على الكتابة فيها).
هل كل شيء يعمل بشكل سلس أمامك؟ إذا واجهت أي ملاحظة في أي خطوة، أخبرني فوراً لأقوم بضبطها.
